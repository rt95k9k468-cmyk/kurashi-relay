# 中継所の合言葉になる道。
#
# GitHubから配置するとき、Cloudflare がこのファイルを読んで
# 「RELAY_PATH は？」と尋ねてきます（Secret として保存されます）。
# くらしノートの 設定 → ダイエット → 中継所 →「道をつくる」で作った
# ものを、そのまま貼ってください。
#
#   例: /kn-pe6twzsqmjaq8kzu
#
# 空のままにすると、中継所は 500 を返して止まります——合言葉の無い
# 郵便受けを、誰でも開ける状態で公開しないためです。
RELAY_PATH=
